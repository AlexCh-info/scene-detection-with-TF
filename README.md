# scene-detection-with-XGBoost
## Автоматическое обнаружение сценовых переходов в видео с помощью ансамбля классических методов и модели XGBoost. (Python)

>⚠️ Внимание: Проект носит исследовательский характер. Модель обучена на 15 видео разной длины (от 1 мин до 1.5 ч). Точность (accuracy) ~0.7. Не предназначена для продакшена.

## 📌 Описание
Проект направлен на сравнение и объединение нескольких методов детекции сценовых переходов в видео:
- `SCD_ebd.ipynb` — Energy-Based Detection (возможно, на основе изменения энергии сигнала)
- `SCD_framediff.ipynb` — Разностный метод (попиксельная разница между кадрами)
- `SCD_hist.ipynb` — Гистограммный метод (сравнение гистограмм цветов/яркости)
- `SCD_mncolor_luminan...` — Метод на основе среднего цвета и яркости
- `SCD_motion_based_full...` — Полный motion-based метод (оптический поток, анализ движения)
- `SCD_motion_based_sh...` — Упрощённый motion-based метод
- `SDC_ssim.ipynb` — Structural Similarity Index (SSIM)

На основе признаков, полученных этими методами, обучена модель XGBoost — `best_model_xgb.pkl`.

## ▶️ Как использовать
1. Установите зависимости
```
pip install opencv-python numpy scikit-image xgboost joblib
```
💡 Если вы используете Jupyter Notebook — убедитесь, что ядро Python имеет доступ к этим пакетам.

2.  Загрузите модель
> Модель сохранена в формате .pkl и загружается через joblib:
```
import joblib 
model = joblib.load('best_model_xgb.pkl')
```
3. Подготовьте данные
Для инференса модель ожидает вектор признаков (например, длиной 15–20), который должен быть сгенерирован одним из методов в *.ipynb файлов.

Пример структуры признаков:
```
features = [
    hist_diff_score,     # из SCD_hist.ipynb
    frame_diff_score,    # из SCD_framediff.ipynb
    ssim_score,          # из SDC_ssim.ipynb
    motion_score_full,   # из SCD_motion_based_full...
    motion_score_short,  # из SCD_motion_based_sh...
    # ... и т.д.
]
```
4. Предсказание
```
prediction = model.predict([features])  # [0] или [1]
probability = model.predict_proba([features])
```
>0 — нет сценового перехода 
>1 — есть сценовый переход
## 🛠️ Как работать с ноутбуками
Каждый .ipynb файл содержит:

- Функции для извлечения признаков из видео
- Примеры использования
- Визуализации

Вы можете:
- Запустить ноутбуки по очереди, чтобы понять логику каждого метода
- Собрать признаки из нескольких методов в один вектор
- Обучить свою модель на новых данных (если нужно)
## 📁 Структура репозитория

```
scene-detection-with-TF/
├── best_model_xgb.pkl        ← предобученная модель XGBoost
├── SCD_ebd.ipynb             ← Energy-Based Detection
├── SCD_framediff.ipynb       ← Frame Difference
├── SCD_hist.ipynb            ← Histogram-based
├── SCD_mncolor_luminan...ipynb ← Mean Color & Luminance
├── SCD_motion_based_full...ipynb ← Full Motion Analysis
├── SCD_motion_based_sh...ipynb ← Simplified Motion
├── SDC_ssim.ipynb            ← SSIM
└── README.md                 ← этот файл
```
# Пример работы программы
<img width="507" height="631" alt="image" src="https://github.com/user-attachments/assets/8fd2839b-e787-402a-a3f2-fbceb5d44aec" /> 










